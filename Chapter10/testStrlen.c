#include <stdlib.h>
#include <stdio.h>
#include <string.h>

int main(void)
{

  char strings[] = {"li da han bu hao."};
  printf("strings length is: %d\n", strlen(strings));
  printf("sizeof(strings) is: %d\n",sizeof(strings));
  return 0;
}